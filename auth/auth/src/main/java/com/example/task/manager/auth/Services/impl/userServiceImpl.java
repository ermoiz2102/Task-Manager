package com.example.task.manager.auth.Services.impl;

import com.example.task.manager.auth.Entities.User;
import com.example.task.manager.auth.Services.userService;
import org.springframework.stereotype.Service;

@Service
public class userServiceImpl implements userService {
    @Override
    public String addUserRequest(User user) {
        return null;
    }
}
